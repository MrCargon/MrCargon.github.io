// Example for Mercury.js
class Mercury extends Planet {
    constructor(scene, resourceLoader, data) {
        super(scene, resourceLoader, data);
        // Mercury-specific initialization
    }
    
    // Mercury-specific methods
}

// IMPORTANT: Make Mercury globally available
window.Mercury = Mercury;