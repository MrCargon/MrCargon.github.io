// Example for Venus.js
class Venus extends Planet {
    constructor(scene, resourceLoader, data) {
        super(scene, resourceLoader, data);
        // Venus-specific initialization
    }
    
    // Venus-specific methods
}

// IMPORTANT: Make Venus globally available
window.Venus = Venus;